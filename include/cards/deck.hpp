#pragma once

/**
 * @file Deck.hpp
 * @brief Declares the Deck class which manages and stores the playable cards of a player.
 */

namespace cards {
  
class Deck {
    public:
        Deck() {};
};
  
} // namespace cards
