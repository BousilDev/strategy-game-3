#include "cards/card.hpp"

namespace cards { 

//TODO: Fix these to be compatible with Tile placement implementation and connect everyting.
// bool BuildingCard::Play(world::Tile& target) {
//     return !(target.get_terrain()->get_name() == "water" || !target.place_building(*building_));
// }

// bool UnitCard::Play(world::Tile& target) {
//     return !(target.get_terrain()->get_name() == "water" || !target.place_unit(*unit_));
// }

} // namespace cards